'use client'

import { Button, Input } from 'antd'

export default function Home() {
  return (
    <div>
      <h1>Página simples e bonita, com funcionalidades complexas e cheia de degradê.</h1>
      <Input placeholder="Digite algo" />
      <Button type="primary">Isso é um botão</Button>
    </div>
  )
}
